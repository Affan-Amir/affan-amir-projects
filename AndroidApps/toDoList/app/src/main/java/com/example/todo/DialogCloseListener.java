package com.example.todo;


import android.content.DialogInterface;

/**
 * Interface that acts as a Dialog Listener
 */
public interface DialogCloseListener {
    public void handleDialogClose(DialogInterface dialog);
}
